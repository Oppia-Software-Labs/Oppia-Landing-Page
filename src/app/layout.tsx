import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: 'Oppia Software Labs - We build decentralized systems designed for real-world impact',
  description: 'Technology that empowers people, builders, and organizations to move forward with confidence.',
  icons: {
    icon: '/oppia-logos/isotypes/isotype-full-color.svg',
    apple: '/oppia-logos/isotypes/isotype-full-color.svg',
  },
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html lang="es" suppressHydrationWarning>
      <body
        className={`${geistSans.variable} ${geistMono.variable} antialiased`}
      >
        {children}
      </body>
    </html>
  );
}
